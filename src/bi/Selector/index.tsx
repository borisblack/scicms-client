import {ISelector} from 'src/types/bi'

interface SelectorProps {
    selector: ISelector
    height: number
}

export default function Selector({selector, height}: SelectorProps) {
    return <></>
}